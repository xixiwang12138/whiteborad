<!--components/real-tab-bar/avatar-display.wxml-->
<view class="container">
  <view class="tab-bar-border"/>
  <view class="menu-item" bindtap="onTap"
        wx:for="{{pages}}" wx:key="index"
        data-item="{{item}}" data-index="{{index}}">
    <view class="tab-bar-item" style="{{item.style}}"/>
<!--    <image class="{{item.command && showMainMenu && 'animation'}}"-->
<!--           src="{{index == curPageIndex ? item.selectedIconPath : item.iconPath}}"-->
<!--           mode="{{item.command ? 'widthFix': 'aspectFit'}}" />-->
    <text style="color: {{curPageIndex === index ? selectedColor : color}}">{{item.text}}</text>
  </view>
</view>
